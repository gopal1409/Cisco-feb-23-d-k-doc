#this will give  you information about docker engine
28  docker info
  ##you can see all the command related to docker
   29  docker
   #we are going to create our first container
   31  docker container run --publish 80:80 --detach nginx
     ##docker ps will show all the running contianer
   32  docker ps
     ##if you create another container with the same image id this is not going to download the image it will give the image from your lcoal repo
   33  docker container run --publish 81:80 --detach nginx
   34  docker ps
   35  docker images
#lets create a new container with apache image this will download the image
   40  docker container run --publish 82:80 --detach httpd
     docker images
     #the above command will show that there is apache image download
     ##now docker ps will show there are three container running
   41  docker ps
#lets try to undestand the purpose of --detach
 docker container run --publish 83:80 nginx
##press ctrl c 
#docker ps this will not show the container
 47  docker ps
#ps -a show running or stopped container
#you will see the continer is in exit state
   48  docker ps -a
#container vs process id 
#lets create a mongo db container
 51  docker run --name mongo -d mongo:8.2-rc-noble
   52  docker ps
#this will show that it is runing as process id
   53  docker top mongo
#top command is an native command in linux which will show all the process id press q to come out
   54  top
#lets check all the process in linux system using ps aux 
   55  ps aux
###docker top mongo will show that mongo db is running using a process id
   56  docker top mongo
###with the kill command replace my process id with your id number
   59  kill -9 8431
   60  docker ps
#this will shwo that the container is in exit state 
   61  docker ps -a
#you can start stop or remove a container 
   62  docker start mongo
   63  docker ps
   64  docker stop mongo
   65  docker rm mongo
   66  docker ps -a
#if you want to see the utilization of all the container cpu ram storage networking
   68  docker container stats
#lets see the docker infomration about the container
  72  docker ps
   73  docker run -p 83:80 -d --name webhost nginx
   74  docker ps
   75  curl localhost:83
   76  docker logs webhost
   77  docker container inspect webhost

